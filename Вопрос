Не получается разобраться и понять почему у меня русский текст ведет себя на вин10 и вин 11 по разному, хотелось придти к единой формулировке и написания кода.

на вин11(чаще ее использую) у меня при компиляции русский текст нормально отображается только с такими параметрами
setlocale(LC_CTYPE, "ru_RU.UTF-8");
SetConsoleCP(CP_UTF8);
SetConsoleOutputCP(CP_UTF8);

Потом как я выяснил, с параметрами выше вин10 выдает иероглифы, НО с параметрами ниже все идеально.
setlocale(LC_CTYPE, "rus");
SetConsoleCP(1251);
SetConsoleOutputCP(1251);

опять же НО, с параметрами ниже (как советовал Владислав Хорев) на вин10 так же все хорошо отображается НО на самой вин 11 это уже не работает
_setmode(_fileno(stdout), _O_U16TEXT);
_setmode(_fileno(stdin), _O_U16TEXT);
wcout L"Какой то текст".... wendl....

опять же, я не знаю, как у вас на проверке отображается русский текст написанный под
setlocale(LC_CTYPE, "ru_RU.UTF-8");
SetConsoleCP(CP_UTF8);
SetConsoleOutputCP(CP_UTF8);

Но на вин11 у меня все идеально.
хотелось бы в дальнейшем не прыгать от одних параметров к другим, что бы на разных виндовс с одинаковыми параметрами все работало корректно
